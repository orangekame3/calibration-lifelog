services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY}
    command: uv run uvicorn app:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
